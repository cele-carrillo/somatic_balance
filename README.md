The style of this site is based of DentistSmile Jekyll Theme. The content is property of Somatic Balance

# Setup
```
bundle install
npm install
npm install -g grunt-cli
```

# Compile SCSS

`grunt sass & grunt autoprefixer`

# Run

`bundle exec jekyll serve`